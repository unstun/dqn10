# 项目状态简报
> 最后更新：2026-04-09 04:00

## 当前在做什么

- Harness 层全面评审完成，.factory/commands/ 和 .factory/skills/ 改为 symlink 指向 .claude/ 待新会话执行
- §4.5 cnn-dqn vs cnn-ddqn 底座消融数据已就位(runs20260408_{dqn,ddqn})，待分析写作

## 关键上下文

- DQN10 从 DQN9 迁移，包名 `ugv_dqn`，远端 GPU 117.50.216.203，项目路径 `$HOME/DQN10/`
- bigmemory 入口：第一句话后自动派 memory-worker(gpt-5.4)；出口：手动 /archive
- .claude/ 是 commands/skills 单一真实源，.factory/ 改为 symlink 引用（单源管理，消除双份维护）
- CLAUDE.md ≡ AGENTS.md，修改任一须同步另一个，并跑 check-agents-sync.sh 验证
- Droid sub-agent 看不到 AGENTS.md（context isolation），规则须写入各 droid 定义文件

## 近期警告

- ⚠ project_truth.md 仍是占位（7行），未填充研究主题/假设/方法，须与 Dr Sun 确认后由 Conductor 填写
- ⚠ symlink 重构待执行：.factory/commands/ 和 .factory/skills/ 改为 symlink，新会话 prompt 已备好
- ⚠ .agents/skills/ 与 .claude/skills/ 存在 13 处差异，用途待确认（对齐还是保留分离？）
- ⚠ .factory/settings.local.json 未创建（个人覆盖文件），MCP 配置两边是否同步未决
