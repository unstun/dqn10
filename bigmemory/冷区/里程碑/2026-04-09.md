# 2026-04-09 重大版本

## bigmemory 记忆系统 v1.0 部署完成
- **达成内容**：完整的按需拉取 + 手动归档记忆系统,双环境(Claude Code + Droid)统一配置
- **关键指标**：8 个 commit,热区 3 文件(≤3700字预算),冷区 5 类按天归档,memory-worker(gpt-5.4)检索 droid
- **下一步**：
  - 补齐 `.factory/settings.local.json`
  - 确认 MCP 配置两边是否需要同步
  - 实际使用中验证 memory-worker 检索效果
  - 考虑 skill 自动触发规则写入 CLAUDE.md
- **关联改动**：bigmemory/、.factory/、.claude/、CLAUDE.md、AGENTS.md

---

## DQN10 三阶段目录结构上线

- **达成内容**：将 DQN10 项目从扁平布局重组为三阶段分层结构（`1_survey/` → `2_experiment/` → `3_paper/`），共 1355 文件 rename；同步更新 CLAUDE.md / AGENTS.md、shell 脚本、Python sys.path、.gitignore
- **关键指标**：两个 commits（pre-flight `a17e9ed` + restructure `f4e5ea9`），编码检验通过（symlink 正确、脚本路径修正验证）
- **下一步**：
  - 更新 bigmemory/冷区/偏好.md 和工作流.md 中路径引用（`configs/` → `2_experiment/configs/`，rsync 目标 → `2_experiment/`）
  - 下次远端训练前确认 rsync 路径已改为 `2_experiment/`
  - 视需要在 `1_survey/` 下建立调研笔记子目录结构
- **关联改动**：`2_experiment/`、`3_paper/`、`1_survey/`、`CLAUDE.md`、`AGENTS.md`、`scripts/`
