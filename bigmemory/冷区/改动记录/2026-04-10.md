# 2026-04-10 改动记录

## 00:20 — dump_conversation.py 双平台重构（Claude Code + Factory/Droid）

- **原因**：原 dump_conversation.py 仅支持 Claude Code JSONL 格式；需同时支持 Factory/Droid 的 JSONL 格式，使 /archive Step 0 在两个平台均可用
- **改动**：
  - 修改 `.claude/scripts/dump_conversation.py`：
    - 新增自动格式检测逻辑（首行 `type: "session_start"` → Droid，`type: "summary"` 或顶层 `role` → Claude Code）
    - 新增 `--source claude|droid|auto` CLI 参数，默认 auto
    - 新增 Droid JSONL 解析器：顶层 `type="message"` + `message.role` 提取 user/assistant
    - 新增 Droid 用户消息逆向 text block 提取（content blocks 列表末尾为真实输入，前面是 system-reminder）
    - Droid subagent 处理：无子目录，每个 subagent 是独立 session 文件（与 Claude Code 的 `<session_id>/subagents/` 不同）
- **训练/推理**：无
- **状态**：已完成，commit 5523eaf

## 01:30 — 新增 llm-local-knowledge-cache/ 调研知识库

- **原因**：Dr Sun 提出 LLM 幻觉严重问题，提出通过本地知识缓存（L1 层）减少幻觉；需系统性摸底现有方案后再决策是否引入、引入哪个
- **改动**：
  - 新建 `.pipeline/survey/llm-local-knowledge-cache/` 调研目录，含：
    - `outline.yaml` — 5个方向 27 items + 执行配置
    - `fields.yaml` — 22个字段定义（背景/质量/技术/集成/实施/结论 共6类）
    - `results/*.json` — 27 个结构化深度调研 JSON，全字段验证通过
    - `generate_report.py` — JSON → Markdown 汇总转换脚本
    - `report.md` — 2311 行汇总报告（27 方案全覆盖）
- **训练/推理**：无
- **状态**：已完成
