# 2026-04-11 改动记录

## 16:00 — Harness 审查修复：commands 路径、droids 对齐、sync-harness 第 4 检、PreToolUse 备份
- **原因**：8 个 `.factory/commands/` 仍指向旧 OMP 架构；`.factory/droids/` 与 `.claude/agents/` 正文与描述漂移（含 "Oh My Paper" 残留）；`sync-harness.sh` 缺少 agents/droids 漂移检测且需兼容 macOS BSD sed；`.factory/settings.json` 需在 Edit/Write 前做 git 备份以与 Harness 安全策略一致
- **改动**：
  - `.factory/commands/`：`plan` / `delegate` / `setup` / `survey` / `experiment` / `write` / `review` / `ideate` 共 8 个 command 从旧 OMP 路径迁移到 DQN10 目录与引用约定
  - `.factory/droids/`：5 个 droid 正文与 `.claude/agents/` 对齐；各 droid `description` 由 "Oh My Paper" 统一为 "DQN10"
  - `.claude/scripts/sync-harness.sh`：新增第 4 项检查（agents vs droids 正文漂移检测）；修复 macOS BSD `sed` 兼容性
  - `.factory/settings.json`：补 `PreToolUse` hook（Edit / Write 前执行 git backup）
- **训练/推理**：无
- **状态**：已完成
