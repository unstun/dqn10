# 2026-04-09 改动记录

## 01:00 — 部署 bigmemory 记忆系统(替代 .pipeline/agent_handoff 体系)
- **原因**：DQN9→DQN10 迁移后记忆系统不完善,agent_handoff.md 无限增长无容量管理
- **改动**：
  - 新增 `bigmemory/` 目录(热区3文件 + 冷区5目录 + 格式规范)
  - 新增 `.factory/droids/memory-worker.md`(gpt-5.4 检索专用)
  - 新增 `.factory/settings.json`(Droid SessionStart hook)
  - 新增 `.factory/commands/`(10个,从 .claude/commands 同步)
  - 新增 `.factory/skills/`(37个,从 .claude/skills 同步)
  - 新增 `.factory/droids/`(6个: memory-worker + 5个 Oh My Paper agents 转 droids)
  - 新增 `.claude/commands/archive.md`(/archive 归档命令)
  - 新增 `.claude/scripts/session-start-lite.sh`(精简版 SessionStart)
  - 修改 `.claude/settings.json`(去掉 Stop hook,SessionStart 指向 lite 版)
  - 修改 `.claude/scripts/stop-check.sh`(改为检查热区 mtime,但后来去掉了 Stop hook)
  - 修改 `CLAUDE.md` + `AGENTS.md`(记忆地图/会话协议/硬规则 #6/#9/记忆系统章节)
  - 归档 `.pipeline/agent_handoff.md` → `.bak`
  - 归档 `.pipeline/experiment_ledger.md` → `.bak`
  - 归档 `.pipeline/literature_bank.md` → `.bak`
- **状态**：已完成,8 个 commit 已 push

## 01:30 — 设计迭代(v1→v2→v3→最终版)
- **原因**：Dr Sun 逐步纠正设计方向
- **改动**：
  - v1: hook sub-agent 入口+出口 → 被否(sub-agent 不可见)
  - v2: 纯 bash dump → 被否(屎山数据污染上下文)
  - v3: 按需拉取 + 主 AI 透明操作 → 批准后再改
  - 最终: 第一句话后自动派 memory-worker + 手动 /archive
- **状态**：已完成

---

## 04:00 — 将 `.agents/` 目录纳入 git 版本控制(snapshot 备份)

- **原因**：Harness 评审会话中决定对 `.claude/commands/` 和 `.factory/commands/` 做 symlink 合并重构；在重构前先用 git commit `c051e87` 保留 `.agents/`(209 files)的完整现状快照，确保重构失败可回退
- **改动**：`git add .agents/ && git commit`，commit `c051e87`，将 `.agents/` 下 209 个文件加入版本追踪
- **训练/推理**：无
- **状态**：已完成；下一步在新会话执行 symlink 重构

## 04:00 — Harness 评审归档 + 热区全量重写

- **原因**：本次会话对 Harness 层做全面评审，发现 .factory/commands|skills 双份维护问题，需记录并更新热区
- **改动**：
  - 热区/状态简报.md 全量重写（添加 symlink 重构待办、.agents/ 差异警告）
  - 热区/未关闭决策.md 新增两条：symlink 合并待执行、.agents/skills 差异待确认
  - 热区/近期改动.md 新增 2026-04-09 Harness 评审条目
- **状态**：已完成

---

## 14:00 — scripts/ 目录重组(平铺 → 3 层结构)

- **原因**：42 个脚本平铺在 scripts/ 下无分类，维护困难；Harness 评审(8/10)后 Dr Sun 要求修复
- **改动**：
  - `scripts/run/`：26 个实验启动脚本（shell run_*/launch_*/auto_* 系列）
  - `scripts/analysis/`：14 个分析脚本（aggregate_*/analyze_*/plot_*/etc.）
  - `scripts/deploy_server.py`：单独保留在顶层（Dr Sun 明确不归入子目录）
  - `sync-harness.sh` 移入 `.claude/scripts/`，与其他 harness 脚本归一
  - 更新 `auto_pipeline_cnn_dqn.sh` 中 2 处功能性路径引用（scripts/ → scripts/analysis/）
- **训练/推理**：无
- **状态**：已完成，commit 3757e47

## 14:00 — 关闭未关闭决策(6 open → 1 open)

- **原因**：Harness 评审积累了 6 条 open 决策，本次会话逐一确认处理
- **改动**：
  - symlink 改造 → 已完成，标 [x]
  - PreCompact 双轨 → 落地，impl 排入 tasks.json medium 优先级
  - MCP 配置同步 → 无需同步（MCP 在全局配置），标 [x]
  - skill 自动触发规则 → 不写入 CLAUDE.md（superpowers 框架处理），标 [x]
  - .agents/skills/ 差异 → 删除整个 .agents/ 目录（190+ 文件，确认只用 Claude Code + Droid）
  - project_truth.md → 唯一仍 open，等 Dr Sun 确认研究主题
  - 热区/未关闭决策.md 全量重写，更新上述关闭状态
- **训练/推理**：无
- **状态**：已完成，commit 3757e47

---

## 04:50 — 三阶段目录重组（DQN10 项目结构扁平 → 分层）

- **原因**：Dr Sun 反思 DQN9 规划/实验/写作混在一起导致思路混乱，决定 DQN10 采用三阶段工作流（规划 → 实验+分析 → 写作），需对应调整目录结构
- **改动**：
  - 新建 `1_survey/`（文献调研阶段），`1_survey/papers/` 为 symlink → `.pipeline/papers/`
  - `ugv_dqn/`、`configs/`、`scripts/`、`train.py`、`infer.py`、`runs*/` → 移至 `2_experiment/`
  - `paper/` → 重命名为 `3_paper/`
  - 修正 `3_paper/` 下 Python 脚本的 `sys.path`（路径层级变化）
  - 更新 DQN10 shell 脚本中路径，新增 `EXP` 变量指向 `2_experiment/`
  - 全面更新 `CLAUDE.md` / `AGENTS.md` 中所有路径引用
  - 更新 `.gitignore`
  - 删除误创建的 `'11'` 文件
  - 共计 1355 文件 rename
- **训练/推理**：无
- **状态**：已完成；commits: `a17e9ed`（pre-flight 快照）、`f4e5ea9`（restructure）

---

## 21:30 — .pipeline/ 从 pipeline 编排系统重构为平文件数据库

- **原因**：Harness 评估发现 .pipeline/ 基本是死的：project_truth.md 只是占位符从未填充，tasks.json 不用，3 个 .bak 文件堆积，研究内容（literature_bank/experiment_ledger）误放在此。原有定位（pipeline 编排）已废弃，需赋予新定位
- **改动**：
  - 删除 `.pipeline/project_truth.md`（占位符，Conductor 概念废弃）
  - 删除 `.pipeline/tasks.json`（不使用）
  - 删除 `.pipeline/agent_handoff.md.bak`、`experiment_ledger.md.bak`、`literature_bank.md.bak`（3 个 .bak 文件）
  - 删除 `.pipeline/research/`（旧调研目录）
  - 新建 `.pipeline/terminology/`（术语库，迁入已有术语表 + README.md）
  - 新建 `.pipeline/literature/`（文献库，index.md + README.md）
  - 新建 `.pipeline/survey/`（综述库，README.md）
  - 新建 `.pipeline/experiments/`（实验台账，README.md）
  - 更新 `.claude/commands/archive.md`：新增 .pipeline/ 分诊（Q7-Q10 文献/实验/调研/术语）+ 多 Agent 并行写入说明 → /archive v2.0
  - 更新 `CLAUDE.md` / `AGENTS.md`：会话启动去掉 tasks.json/project_truth 引用，记忆地图 .pipeline/ 节改写，硬规则 #28 从"不改 project_truth"改为"不改 .pipeline/ 结构"，记忆系统出口增加 .pipeline/ 维护和模型说明
  - 运行 `check-agents-sync.sh` 校验通过
- **训练/推理**：无
- **状态**：已完成

---

## 00:20 — /archive 升级至 v3.0（协调 Agent + 5 Worker 并行架构）

- **原因**：v2.0 主 AI 仍需亲自执行所有写入步骤，效率低；Dr Sun 明确要求"主 AI 最少参与，全部下沉到协调 agent"
- **改动**：
  - 更新 `.claude/commands/archive.md` → /archive v3.0：主 AI 职责收缩为 3 步（Step 0 导出+Step 1 启动协调 Agent+Step 2 转发报告），全部归档逻辑移交协调 Agent
  - 协调 Agent 负责：读取热区状态 + 分诊（Q1-Q10）→ 并行派 5 个 Worker（sonnet）→ git 备份 → 输出报告
  - Worker 分工：Worker1 冷区事实层 / Worker2 冷区认知层 / Worker3 冷区决策层 / Worker4 热区瘦身 / Worker5 .pipeline/+冷区降级
  - 新增 Step 0：`dump_conversation.py` 自动从 JSONL 解析导出会话记录到 `bigmemory/冷区/会话记录/YYYY-MM-DD_HHMM.md`
  - 新增 **模型硬约束**：Claude Code 协调 Agent + 5 Workers 均用 sonnet，Droid 均用 gpt-5.4-mini（Dr Sun 担心偷用 opus）
  - 新增 `.claude/scripts/dump_conversation.py`：读取 `~/.claude/projects/` 下 JSONL，解析 user/assistant 消息，跳过 system-reminder/meta，包含主会话 + 所有 subagent 对话
  - 进一步更新 `CLAUDE.md` / `AGENTS.md`：记忆系统出口增加多 Agent 并行 + 模型硬约束说明，硬规则与 v3.0 架构同步
  - `check-agents-sync.sh` 校验通过，commits: c0314e8（snapshot）、3753d81（v2.0）、9fb2f63（v3.0）
- **训练/推理**：无
- **状态**：已完成
